[pytest]
# Each service's tests must run in isolation (all services share the `app`
# package name). Use scripts/run_tests.py for the full suite. Per-service
# runs (cd services/<svc> && pytest) work without any flags.
